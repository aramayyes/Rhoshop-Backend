type User {
  id: String!
  name: String!
  phoneNumber: String!
  email: String!
}

input CreateUserInput {
  name: String!
  phoneNumber: String!
  email: String!
  password: String!
}

extend type Mutation {
  createUser(createUserInput: CreateUserInput!): User
}
